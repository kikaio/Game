#pragma once

enum class ERR_CATEGORY {
	NONE = 0
	, DB
};

enum class DB_ERR_DETAIL {
	CONN_FAILED = 0
	, CONN_TIMEOUT
	, INVALID_PARAM
	, NOT_EXIST_ACCOUNT
	, DUPLICATE_ACCOUNT_ID
	, PROCEDURE_FAILED
	, CONNECTION_NOT_EXIST
	, RESULT_SET_MISMATCH
	, EXECUTE_FAILED
};

