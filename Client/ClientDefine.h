#pragma once





DECL_REF_USING(GameServerSession);
DECL_WREF_USING(GameServerSession);

DECL_REF_USING(ChatServerSession);
DECL_WREF_USING(ChatServerSession);

DECL_REF_USING(DumAct);
DECL_WREF_USING(DumAct);

DECL_REF_USING(DummyUser);
DECL_WREF_USING(DummyUser);

DECL_REF_USING(Logger);
DECL_WREF_USING(Logger);


DECL_REF_USING(ChatProfile);
DECL_WREF_USING(ChatProfile);

DECL_REF_USING(ChatData);
DECL_WREF_USING(ChatData);

